<input type="hidden" name="value" id="value" value=@_value.ToArgb() />
<button class="color-picker-trigger" style="background-color: @_value.ToRgbaString()"></button>

<dialog class="color-picker-dialog">
    <fig
</dialog>
<p role="status">Current count: @currentCount</p>

<button class="btn btn-primary" @onclick="IncrementCount">Click me</button>

@code {
    private Color _value;

    private int currentCount = 0;

    private void IncrementCount()
    {
        currentCount++;
        Console.WriteLine($"Increment {currentCount}");
        Debug.WriteLine($"Increment {currentCount}");
    }

}